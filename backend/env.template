# Database Configuration
DB_HOST=postgres-service
DB_PORT=5432
DB_NAME=counterdb
DB_USER=postgres
DB_PASSWORD=password

# Server Configuration
PORT=3001

# Note: In Kubernetes, these values will be set via environment variables
# or ConfigMaps/Secrets in the deployment manifests
